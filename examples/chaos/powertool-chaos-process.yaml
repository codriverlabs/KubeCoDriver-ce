apiVersion: kubecodriver.codriverlabs.ai/v1alpha1
kind: CoDriverJob
metadata:
  name: chaos-process-suspend
spec:
  targets:
    labelSelector:
      matchLabels:
        app: my-application
  tool:
    name: "chaos"
    duration: "2m"
    args:
      - "suspend"
  output:
    mode: "ephemeral"
