{{- if .Values.collector.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubecodriver-collector
  namespace: {{ include "kubecodriver-operator.namespace" . }}
  labels:
    {{- include "kubecodriver-operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector
spec:
  replicas: {{ .Values.collector.replicas }}
  selector:
    matchLabels:
      app: kubecodriver-collector
  template:
    metadata:
      labels:
        app: kubecodriver-collector
        {{- include "kubecodriver-operator.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: kubecodriver-collector
      {{- if .Values.collector.storage.fsGroup }}
      securityContext:
        fsGroup: {{ .Values.collector.storage.fsGroup }}
      {{- end }}
      {{- with include "kubecodriver-operator.imagePullSecrets" . }}
      imagePullSecrets:
{{ . | indent 8 }}
      {{- end }}
      containers:
      - name: collector
        image: {{ include "kubecodriver-operator.collector.image" . }}
        imagePullPolicy: {{ .Values.collector.image.pullPolicy }}
        ports:
        - containerPort: 8443
          name: https
        env:
        - name: TLS_CERT_PATH
          value: "/etc/certs/tls.crt"
        - name: TLS_KEY_PATH
          value: "/etc/certs/tls.key"
        volumeMounts:
        - name: data
          mountPath: /data
        - name: certs
          mountPath: /etc/certs
          readOnly: true
        resources:
          {{- toYaml .Values.collector.resources | nindent 10 }}
      volumes:
      - name: data
        {{- if .Values.collector.storage.enabled }}
        persistentVolumeClaim:
          claimName: kubecodriver-collector-data
        {{- else }}
        emptyDir: {}
        {{- end }}
      - name: certs
        secret:
          secretName: kubecodriver-collector-certs
---
apiVersion: v1
kind: Service
metadata:
  name: kubecodriver-collector
  namespace: {{ include "kubecodriver-operator.namespace" . }}
  labels:
    {{- include "kubecodriver-operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector
spec:
  selector:
    app: kubecodriver-collector
  ports:
  - port: {{ .Values.collector.service.port }}
    targetPort: 8443
    name: https
  type: {{ .Values.collector.service.type }}
{{- if .Values.collector.storage.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubecodriver-collector-data
  namespace: {{ include "kubecodriver-operator.namespace" . }}
  labels:
    {{- include "kubecodriver-operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.collector.storage.size }}
  {{- if .Values.collector.storage.storageClass }}
  storageClassName: {{ .Values.collector.storage.storageClass }}
  {{- end }}
{{- end }}
{{- end }}
