apiVersion: kubecodriver.codriverlabs.ai/v1alpha1
kind: CoDriverTool
metadata:
  name: aperf-config
  namespace: kubecodriver-system
spec:
  name: aperf
  image: ghcr.io/codriverlabs/kubecodriver-aperf:latest
  securityContext:
    privileged: true
    capabilities:
      add: ["SYS_ADMIN", "SYS_PTRACE"]
